# Notebooks

Esta carpeta queda reservada para exploración visual, análisis de datos y prototipos.

La lógica principal del proyecto debe vivir en `src/` y `scripts/`; los notebooks no deben contener código imprescindible para entrenar, inferir o servir la API.
