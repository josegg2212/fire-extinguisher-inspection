# Datos locales

- `yolo/`: dataset YOLO final.
- `classifier_context_final_v7/`: dataset final de la CNN, copiado sin symlinks.
- `raw/`: entrada temporal para pruebas manuales nuevas.

Los datasets intermedios se eliminaron para evitar duplicados. La trazabilidad resumida queda en `docs/14_cierre_final.md` y `outputs/final_validation/`.
